int main() {
    int joel;
    int x;
    joel = x + x;
    int y;
    int z;
    if((x + y) && z) {
        x = func(j, x);
        func(j, 3.4);
        while(!(x == 1)) {
            output(j);
            char bluh;
        }
    }
    return joel;
}

int func(char a, char b, char c) {
    int y;
}