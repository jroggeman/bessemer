int main() {
    int joel;
    joel = x + x;
    if(x + y) {
        x = func(j, x);
        func(j, x);
        while(x + x) {
            print(j);
        }
    }
    return joel;
}

int x() {
    int y;
}