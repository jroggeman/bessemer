int main() {
    int joel;
    joel = x + x;
    if(x + y) {
        x = func(j, x);
        func(j, 3.4);
        while(x + 1) {
            print(j);
        }
    }
    return joel;
}

int x() {
    int y;
}